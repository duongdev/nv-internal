git_url("git@github.com:duongdev/nv-internal-certificates.git")

storage_mode("git")

type("appstore")

app_identifier(["vn.dienlanhnamviet.internal"])

# Use App Store Connect API Key instead of username/password
# This is configured via load_asc_api_key in Fastfile
# username("dustin.do95@gmail.com")

team_id("9F77J83SKT")

# For CI/CD
readonly(ENV['CI'] == 'true')
