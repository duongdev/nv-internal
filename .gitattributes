# Git LFS - Track large binary files
# https://git-lfs.github.com/

# Encrypted backup files (if stored locally for testing)
*.gpg filter=lfs diff=lfs merge=lfs -text

# Compressed SQL dumps
*.sql.gz filter=lfs diff=lfs merge=lfs -text

# Tarball archives
*.tar filter=lfs diff=lfs merge=lfs -text

# Handle line endings for cross-platform compatibility
* text=auto

# Explicitly declare text files
*.md text
*.txt text
*.json text
*.js text
*.ts text
*.tsx text
*.css text
*.scss text
*.html text
*.yml text
*.yaml text
*.sh text eol=lf

# Binary files
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.woff binary
*.woff2 binary
*.ttf binary
*.eot binary
